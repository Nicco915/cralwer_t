version: "3.8"

services:
  crawler:
    image: ${CRAWLER_IMAGE}
    container_name: hs-sku-crawler
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env:ro
      - ./logs:/app/logs
      - ./output:/app/output
      - ./images:/app/images
    working_dir: /app
